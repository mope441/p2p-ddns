当前项目：mope441/p2p-ddns，分支 openclaw-plugin。

请暂停新功能开发，先完成一轮“可部署、可运行、可端到端验收”的修复。目标是让 1Panel Docker OpenClaw 场景下的 p2p-ddns 教学班级功能可以最小闭环跑通。

## 一、先修阻塞性 bug

### 1. 修复 class create 初始化权限循环

当前现象：

```bash
p2p-ddnsctl --admin-http 127.0.0.1:8080 class create "AI"
```

返回：

```text
Error: Not a class member
```

原因：`src/admin/handler.rs` 中 `ClientCommand::ClassCreate` 分支一开始调用了 `require_teacher(ctx, client_id)`。但 `class create` 是创建第一个班级的初始化操作，此时 class_store 中还没有 teacher member，所以必然失败。

修复要求：

- `ClassCreate` 不要调用 `require_teacher`。
- `ClassCreate` 只检查：
  - 当前 daemon 是 primary，即 `ctx.args.primary == true`
  - 当前还没有 class network
- 创建成功后继续保存：
  - `ClassNetwork`
  - teacher member，`node_id = ctx.me.node_id`
  - `role = Teacher`
  - `status = Approved`

### 2. 修复 teacher 权限模型

当前问题不只是 `class create`。HTTP admin CLI 会生成一个 CLI client public key，`require_teacher(ctx, client_id)` 查的是这个 CLI client id。但 class create 写入的 teacher 是 daemon 自己的 `ctx.me.node_id`，所以后续 teacher-only 操作仍会因为 CLI client 不是 class member 而失败。

修复要求：

- 将 teacher-only 权限检查改为基于 daemon 身份，而不是 CLI client 身份。
- 建议新增函数：

```rust
fn require_teacher_daemon(ctx: &Context) -> Result<(), String>
```

逻辑：

- 从 `class_store` 中加载 `ctx.me.node_id` 对应的 member。
- member 必须存在。
- member.role 必须是 `Teacher`。
- member.status 必须是 `Approved`。

对于本地 admin-http 或本地 socket CLI，teacher-only 操作应代表当前 daemon 执行。不要用 CLI client public key 判断 teacher 身份。

需要替换以下 teacher-only 操作的权限检查：

- ClassInviteCreate
- ClassJoinRequestsApprove
- ClassJoinRequestsDeny
- ClassGroupCreate
- ClassGroupAddMember
- ClassGroupRemoveMember
- ClassGroupDelete
- ClassBroadcast
- ClassMulticast
- ClassDirectSend

其中 group add/remove、multicast、direct send 当前也要补上 teacher 权限检查。

### 3. 修复 class join 端到端流程

当前 `ClassJoin` 在 student 节点本地查 `pending_invite`，这是错误的。pending invite 是 teacher 侧生成并保存的，student 本地没有这个记录，所以 student 执行 join 会返回 `Unknown invite ID`。

请实现一个最小可用的 teacher/student join 闭环：

#### Invite

`JoinTicket` 中需要包含足够让 student 提交请求的信息。建议加入：

```rust
display_name: String
```

teacher 生成 invite 时，把 `display_name` 写入 invite JSON。

#### Student join

student 执行：

```bash
p2p-ddnsctl --admin-http 127.0.0.1:8080 class join /tmp/student-one-invite.json
```

应执行：

1. 解析 invite JSON。
2. 校验 `expires_at`。
3. 解析 invite 中的 `p2p_ticket`。
4. 将 teacher 节点加入本地 trusted/active nodes。
5. 保存必要的 class metadata，例如 class_id、class_name、teacher_node_id、teacher_domain、shared_secret。
6. 通过 P2P 消息向 teacher 发送 join request。

#### P2P message

在 `src/domain/message.rs` 中新增消息类型，例如：

```rust
ClassJoinRequest {
    invite_id: String,
    class_id: TopicId,
    student_node_id: EndpointId,
    display_name: String,
    timestamp: u64,
}
```

teacher 收到 `ClassJoinRequest` 后：

1. 根据 invite_id 查 teacher 本地 pending invite。
2. 检查 invite 未过期。
3. 检查 class_id 匹配。
4. 创建 `ClassMember`：
   - node_id = student_node_id
   - display_name = invite/pending 中的 display_name
   - role = Student
   - status = Pending
5. 删除或标记 consumed invite。
6. 写 audit log。

#### Teacher approve

teacher 执行：

```bash
p2p-ddnsctl --admin-http 127.0.0.1:8080 class join-requests list
p2p-ddnsctl --admin-http 127.0.0.1:8080 class join-requests approve <student-node-id>
```

应能看到 pending student，并将其状态改成 Approved。

### 4. 修复 invite `--output`

当前 CLI 定义了 `--output`，但命令转换时没有使用它。

修复要求：

- 如果用户传了 `--output /path/invite.json`，CLI 应把 invite JSON 写到该文件。
- 如果没有传 `--output`，继续打印到 stdout。
- 不要把 `--json` 输出包一层后导致用户难以直接给 student 使用。
- 文档和提示要与真实命令一致。当前实际命令是：

```bash
class join <invite_file>
```

不要提示成不存在的：

```bash
class join --invite <file>
```

### 5. 修复旧数据库兼容问题

当前 Node 结构增加了 `role: Option<NodeRole>`，但 Node 是用 postcard 二进制格式持久化的，旧数据库会出现：

```text
Hit the end of buffer, expected more data
```

修复要求二选一：

优先方案：

- 从持久化 `Node` 结构中移除 `role` 字段。
- 教学角色只保存在 `ClassMember`。
- P2P 节点发现层不要耦合教学角色。

如果确实要保留 Node.role：

- 实现版本化存储或兼容反序列化。
- 新增测试：旧格式 Node 数据可以被新版读取，并且 role 默认为 None。

### 6. 明确 message status / outbox 能力

当前 broadcast/multicast/direct send 返回结果里没有 message_id，message status 很难测试。outbox retry 也只是更新状态，没有真正重发。

请做一个明确选择：

方案 A：本阶段暂不实现可靠消息

- 删除或隐藏 `class message status/retry` 命令。
- 或返回明确错误：`message status is not implemented in this phase`。

方案 B：实现最小可用可靠消息

- BroadcastResult 返回 `message_id`。
- direct send / broadcast / multicast 保存 outbox record。
- message status 可以查到该 message_id。
- retry 至少能重新调用发送逻辑，不能只改状态。

优先建议选方案 A，先保证教学班级主流程可用。

## 二、增加一键部署脚本

当前部署步骤太散，请新增脚本，适配用户的 Linux VM + 1Panel Docker OpenClaw 场景。

### 1. `scripts/update-linux.sh`

功能：

- 默认 repo：`https://github.com/mope441/p2p-ddns.git`
- 默认 branch：`openclaw-plugin`
- 默认 repo dir：`/opt/p2p-ddns-mope-test`
- 如果目录不存在或不是 git repo，则重新 clone。
- 如果目录存在且是 git repo，则 fetch + checkout + pull。
- 运行：
  - `cargo fmt --all -- --check`
  - `cargo test --all-targets --no-run`
  - `cargo build --release --bins`
- 备份旧二进制到 `/opt/p2p-ddns-backup/<timestamp>/`
- 停止：
  - `p2p-ddns-agent-transport`
  - `p2p-ddns`
- 安装：
  - `target/release/p2p-ddns` 到 `/usr/local/bin/`
  - `target/release/p2p-ddnsctl` 到 `/usr/local/bin/`
  - `target/release/p2p-ddns-agent-transport` 到 `/usr/local/bin/`
- 重启：
  - `p2p-ddns`
  - `p2p-ddns-agent-transport`
- 最后验证：
  - `p2p-ddnsctl --admin-http 127.0.0.1:8080 status`
  - `p2p-ddnsctl --admin-http 127.0.0.1:8080 class --help`

要求：

- 不要用 `chmod 777`。
- clone 后用 `chown -R $USER:$USER` 修复源码目录权限。
- 判断仓库时检查 `.git`，不要只判断目录存在。

### 2. `scripts/configure-openclaw-1panel.sh`

功能：

- 支持通过参数指定 OpenClaw 容器名，默认自动从 `docker ps` 中查找包含 `claw` 或 `openclaw` 的容器。
- 通过 `docker inspect` 自动获取：
  - compose working dir
  - 映射到 `/home/node/.openclaw` 的宿主机目录
- 将插件复制到：

```bash
<OPENCLAW_CONFIG_DIR>/extensions/openclaw-p2p-ddns-lan
```

- 不要把 `/home/node/.openclaw` 当成宿主机路径。
- 复制后执行：

```bash
docker restart <container>
```

- 验证：
  - 容器内能看到插件目录
  - `openclaw plugins list`
  - `openclaw directory peers list --channel p2p-ddns-lan`

### 3. `scripts/diagnose.sh`

输出诊断信息：

- `systemctl status p2p-ddns`
- `systemctl status p2p-ddns-agent-transport`
- `journalctl -u p2p-ddns -n 80`
- `journalctl -u p2p-ddns-agent-transport -n 80`
- `systemctl cat p2p-ddns`
- `systemctl cat p2p-ddns-agent-transport`
- `/etc/p2p-ddns/p2p-ddns.env`
- `/etc/p2p-ddns/agent-transport.env`
- `ss -lntup | grep -E '8080|39091|7777'`
- `ss -lunp | grep 7777`
- Docker OpenClaw container/mounts
- 宿主机 curl：
  - `curl -i http://127.0.0.1:39091/health`
  - `curl -i http://127.0.0.1:39091/contacts`
- 容器内 curl：
  - `curl -i http://host.docker.internal:39091/health`

敏感信息要求：

- 打印 env 时，shared secret 和 ticket 要打码，例如只显示前 8 位和后 6 位。

## 三、增加最小端到端测试脚本

新增：

```bash
scripts/smoke-class-teacher.sh
scripts/smoke-class-student.sh
```

### teacher smoke

执行：

```bash
p2p-ddnsctl --admin-http 127.0.0.1:8080 class create "AI"
p2p-ddnsctl --admin-http 127.0.0.1:8080 class info
p2p-ddnsctl --admin-http 127.0.0.1:8080 class members list
p2p-ddnsctl --admin-http 127.0.0.1:8080 class invite create --display-name "Student One" --expires 86400 --output /tmp/student-one-invite.json
```

要求：

- class create 成功。
- members list 显示 teacher approved。
- invite 文件真实生成。

### student smoke

执行：

```bash
p2p-ddnsctl --admin-http 127.0.0.1:8080 class join /tmp/student-one-invite.json
```

要求：

- student join 不应该返回 Unknown invite ID。
- teacher 侧应能在 join-requests list 中看到 student pending。

## 四、必须新增或更新测试

至少增加以下测试：

1. fresh primary daemon 可以 class create 成功。
2. class create 后 teacher member 是 `ctx.me.node_id`，role=Teacher，status=Approved。
3. teacher local admin 可以创建 invite。
4. student join 不查本地 pending invite。
5. teacher 收到 join request 后能创建 pending ClassMember。
6. approve 后 student status 从 Pending 变成 Approved。
7. 旧 Node postcard 数据不会导致 daemon 启动时报 `Hit the end of buffer`。
8. group add/remove、multicast、direct send 必须执行 teacher 权限检查。
9. invite `--output` 确实写文件。

## 五、验收命令

修完后必须运行：

```bash
cargo fmt --all -- --check
cargo test --all-targets
cargo build --release --bins
```

然后在两台 Linux VM 上验证：

teacher：

```bash
p2p-ddnsctl --admin-http 127.0.0.1:8080 class create "AI"
p2p-ddnsctl --admin-http 127.0.0.1:8080 class invite create --display-name "Student One" --expires 86400 --output /tmp/student-one-invite.json
```

student：

```bash
p2p-ddnsctl --admin-http 127.0.0.1:8080 class join /tmp/student-one-invite.json
```

teacher：

```bash
p2p-ddnsctl --admin-http 127.0.0.1:8080 class join-requests list
p2p-ddnsctl --admin-http 127.0.0.1:8080 class join-requests approve <student-node-id>
p2p-ddnsctl --admin-http 127.0.0.1:8080 class members list
p2p-ddnsctl --admin-http 127.0.0.1:8080 class broadcast --message "hello class"
```

最终验收标准：

- 一键更新脚本可用。
- 1Panel Docker OpenClaw 插件自动复制和重启可用。
- class create 不再报 Not a class member。
- invite 文件真实生成。
- student join 不再报 Unknown invite ID。
- teacher 能看到 pending join request。
- approve 后 student 为 Approved。
- broadcast 至少能对 approved student 返回 sent 成功。
- 旧数据库兼容问题有明确修复或迁移方案，不再直接启动失败。